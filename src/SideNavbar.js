import React from 'react';
import './Social.css';

function SideNavbar({ position }) {
    return (
        <div className={`side-navbar ${position}`}>
            {/* Add side navbar content here */}
        </div>
    );
}

export default SideNavbar;
